{
  "schema": "aea.organism.catalogue/1",
  "note": "THE PARTS, AS DATA. Moved out of Python 2026-07-27 so a creature is a file rather than an argument list. Every field here is load-bearing at run time; nothing is documentation.",
  "stages": ["shape", "fire", "read", "repair", "carry", "judge"],
  "stage_note": "The pipeline runs stages in this order, and WITHIN a stage in ascending `order`. That within-stage number is the wiring the old code did not declare: x16 lost 18 points because validation handed control to the readout at the same stage and nothing said which went first.",
  "kinds": {
    "enabling": "without it there is no organism. Not scored.",
    "lever": "claims to change the ANSWER. Scored on accuracy.",
    "gauge": "claims to change what can be KNOWN, not what is answered. Scoring it on accuracy is a category error and this project made it repeatedly.",
    "guard": "claims to convert silent wrongness into visible abstention. Scored on false-commitment rate.",
    "channel": "claims to make a state legible in a non-textual signal. Scored on separability."
  },
  "components": [
    {
      "key": "call", "name": "THE CALL", "census": "C-62",
      "stage": "fire", "order": 1, "kind": "enabling", "metric": null,
      "requires": [],
      "receipt": "without it there is no organism; every version contains it"
    },
    {
      "key": "goal", "name": "THE GOAL", "census": "C-12",
      "stage": "shape", "order": 1, "kind": "lever", "metric": "accuracy",
      "requires": ["call"],
      "receipt": "x12: 0 of 158 clean trials succeed with no objective, 0 of 146 more with a vague one"
    },
    {
      "key": "frame", "name": "THE METHOD FRAME", "census": "C-04",
      "stage": "shape", "order": 2, "kind": "lever", "metric": "accuracy",
      "requires": ["call"],
      "receipt": "x15: working 41% -> 100%, four rods 0.00 -> 1.00. A METHOD frame only",
      "warning": "a frame naming only a MANNER is the most harmful thing measured in World 1: 8/12 -> 0/6"
    },
    {
      "key": "readout", "name": "THE READOUT", "census": "C-87?",
      "stage": "read", "order": 1, "kind": "lever", "metric": "accuracy",
      "requires": ["call"],
      "receipt": "x13: recovers 3 of 153 mute replies at zero tokens. x18 scores it inert overall, median +0.04",
      "warning": "a method frame ELIMINATES the condition this repairs: mute goes 28/288 bare to 0/360 fitted"
    },
    {
      "key": "validation", "name": "THE VALIDATION GUARD", "census": "C-75",
      "stage": "read", "order": 2, "kind": "guard", "metric": "false_commitment_rate",
      "requires": ["call"],
      "receipt": "x17: on the rod that needs nothing, 7/7 -> 0/7, every loss a forced abstention",
      "warning": "SCORED ON ACCURACY IT LOOKS HARMFUL. It is a guard. Converting a silent wrong answer into a visible abstention is the job, not the failure."
    },
    {
      "key": "critic", "name": "THE CRITIC", "census": "C-14",
      "stage": "repair", "order": 1, "kind": "lever", "metric": "accuracy",
      "requires": ["call"],
      "receipt": "x20: 18 paired cells. Upside bounded at +0.12, inside the band. Losses to -0.55, all on high baselines. Cost 1.18x",
      "warning": "it breaks what was already right. Seat when stuck, remove when not"
    },
    {
      "key": "carry", "name": "THE CHECKPOINT", "census": "C-80",
      "stage": "carry", "order": 1, "kind": "lever", "metric": "accuracy_over_sequence",
      "requires": ["call"],
      "receipt": "x06b: 11/11 vs 9/16, p=0.0216 at 50x calls. ONE task at ONE chain length",
      "warning": "UNMEASURED as of 2026-07-27. x21's control passed the running value to the no-carry arm, so the baseline contained the treatment and the -0.09 result is void. organism.py:431"
    },
    {
      "key": "measure", "name": "THE MEASURE", "census": "C-15",
      "stage": "judge", "order": 1, "kind": "gauge", "metric": "can_know",
      "requires": ["call"],
      "receipt": "x13: fooled 0 times in 153. It changes no answer and changes what can be known",
      "warning": "reporting this as inert on accuracy is the category error, not a finding"
    },
    {
      "key": "latency", "name": "THE CLOCK", "census": "C-74",
      "stage": "judge", "order": 2, "kind": "channel", "metric": "separability",
      "requires": [],
      "receipt": "x13: 1.97s right vs 16.12s wrong, ratio 8.18 on one rod and 0.95 on another",
      "warning": "requires nothing beneath it and depends on everything. Two edges, one word"
    }
  ]
}
