# REFERENCES — where the authoritative sources live

Per Luis's call: **pointers, not copies.** This maps each reference type to its authoritative
source so a takeover agent (or a new conversation) finds truth without hunting — and without
dragging private material into this repo. In-repo sources are linked directly; external ones are
named by document, never by an absolute path (the PORTFOLIO project sits under a work OneDrive whose
folder name is the employer — that path never gets written here).

## AEA FRAMEWORK — the architecture the game implements
- **In-repo (primary):** `design/A15_FULL_COVERAGE.md` (the 86-item canonical census),
  `design/A8_AEA_ALIGNMENT.md`, `design/A16_WIRTHFORGE.md`, and the live element graph
  `web/aea_elements.js` + `state/codex_index.json`.
- **External:** the AEA master document in Luis's PORTFOLIO project (the canonical axes / seeds /
  mechanics / ops / innovation-layer definition). Pointer only.

## SITE AEA EXPLAINER / POSITIONING — how the AEA is framed publicly
- **External:** `LUISBLANCODEV_MASTER.md` in the PORTFOLIO project (the site definition — the Four
  Levels of AI Systems, the anti-evangelism stance, the "found meaning in mechanism" framing).
  Pointer only. This is the *public* voice the game's argument should stay consistent with.

## VISUAL-LANGUAGE SPEC — the locked look
- **In-repo (primary):** `design/E2_VISUAL_DIRECTION.md`, `design/E8_FIDELITY_LAW.md`,
  `design/A11_SIGNATURE.md` (the two-ink law, the render doctrine, the fidelity gates).
- **External:** the PORTFOLIO site's locked visual language (§6 of the site master — palette, type,
  motifs). Pointer only; the game's look and the site's are meant to rhyme.

## PRIVACY GUARD (standing)
No employer/client/internal-system names, no NDA/multi-employment references, no compensation data,
no absolute filesystem paths, no personal identifiers enter this repo. If any external reference is
ever *copied* in rather than pointed at, it is privacy-scanned first (the guard that caught
`data.js` leaking <REDACTED-EMPLOYER>/Amazon/NDA during the AEA_GAME backup).
