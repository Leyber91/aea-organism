# 11 · THE RUN TRACE — one frame of the finished game (the receipt column closing) · GPT-Image-2 spec v2

The moment the measure closes the wire: the full receipt column of a real run printing line by
line, ending in PASS and a standing RECORD. Honest telemetry as drama — every line is something
that really happened. A close crop of the bench's lower region.

## Workflow for this frame

1. Same conversation as REF-10 → attach NOTHING (reference-reuse). Fresh conversation → attach
   REF-10 THE BENCH PLATE once.
2. Paste THE PROMPT below as one message. High quality (dense text).
3. ONE image back → fix one quoted string per message → accept as `11_THE_RUN_TRACE_<take>.png`
   → REF-11.

## THE PROMPT (paste as one message, no attachment needed if in the same conversation)

Match the EXACT style, material and type of the bench plate you generated earlier in this
conversation — matte near-black machined plate, debossed monospace engraving, tabular numerals,
milled hairlines, amber only as emitted light on state words. This is a close crop of that same
bench's lower region. No people, no watermark, no gloss, no rounded UI, no new elements.

SCENE
A close, straight-on crop of the lower bench plate at the instant a run closes: the same
machined near-black surface, faint horizontal hairline rules aligning the lines of text, vast
quiet plate margins around a single column of receipt lines. Everything debossed into the metal.

SUBJECT — the receipt column, printing downward, OLDER LINES DIM, NEWER LINES BRIGHTER, all
debossed monospace capitals; ONLY the state words carry amber:
1. "IGNITE c-07 · t-04 GROUNDED ANSWER"
2. "DRAW · ZONE PRIVATE · EST FREE"
3. "HOP granite -> FELL THROUGH"   (FELL THROUGH in warm amber)
4. "HOP qwen2.5 · 13.2s COLD"
5. "MEM 3 RECALLED"
6. "THE MEASURE · 4 AXES"
7. "PASS · 2.08s · FREE"           (PASS in hot amber — the brightest element in frame)

THE CLOSE, beneath line 7:
- one thin milled amber rule (light in the groove) spanning the column width
- beneath it a record stamp: "RECORD · LAST 2.08S · BEST 0.98S" beside one small filled amber
  square debossed into the plate

THE TIME RULER, right margin, vertical: a thin milled hairline with small graduation ticks,
labeled top to bottom "T+0.0" "T+0.5" "T+1.0" "T+1.5" "T+2.0".

KEY DETAILS
- The recency gradient (dim old -> bright new) carries the eye down to the hot PASS — a
  seismograph of a mind at work: quiet, quiet, then the amber strike.
- Amber appears ONLY on "FELL THROUGH", "PASS", the closing rule, and the record square.
- Tabular numerals, strict left alignment, engineer's grid.

CONSTRAINTS (hard)
1. Landscape 1536×1024, high quality (dense small text), straight-on close crop.
2. Exactly SEVEN trace lines in the order given, then the rule, the record stamp, the ruler.
3. "PASS · 2.08s · FREE" is the brightest element; "FELL THROUGH" the only other amber text; the
   rest debossed structure-grey with a dim-to-bright recency gradient.
4. Every quoted string rendered once, verbatim, legible.
5. No people, no watermark, no gloss, no progress bars or spinners, no new elements.

## Ready-made refinement lines

- "Change only the recency gradient: top line dimmer, PASS brighter. Keep everything else the same."
- "Change only PASS: hotter amber, the single brightest thing. Keep everything else the same."
- "Violation — line 4 must read exactly 'HOP qwen2.5 · 13.2s COLD'. Correct that string only, change nothing else."
- "Count violation — exactly seven trace lines in the given order. Regenerate with seven, everything else the same."

## What this frame must make a stranger feel

The tension of honest telemetry: *this really ran, step by step, and it PASSED.* If it reads as a
log dump, it fails; if the column reads as a heartbeat ending in a struck verdict, it passes.
