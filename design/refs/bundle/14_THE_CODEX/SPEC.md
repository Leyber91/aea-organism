# 14 · THE CODEX — one frame of the finished game (the pokédex of the AEA) · GPT-Image-2 spec

The catalog of everything the game is made of: a specimen wall of the AEA elements — the organs,
the parts, the doctrines — each an engraved card, DISCOVERED cards lit, undiscovered cards fog.
This is the answer to "how the pokédex menu looks" and the surface that finally shows "so many
elements." (Second-wave frame: the meta layer.)

## Workflow

1. Same conversation as REF-07/10 (the bench chip + plate material) → attach NOTHING. Fresh
   conversation → attach REF-10 THE BENCH PLATE once (the octagonal chip + machined plate canon).
2. Paste THE PROMPT below as one message. High quality (dense text). ONE image → fix one string
   per message → accept as `14_THE_CODEX_<take>.png` → REF-14.

## THE PROMPT (paste as one message)

Match the EXACT material and type of the bench plate you generated earlier in this conversation —
matte near-black machined plate, octagonal corner-cut cards, debossed monospace engraving, amber
only as emitted light on discovered items. No people, no watermark, no gloss, no new elements.

SCENE
The codex screen: one large machined near-black instrument plate photographed face-on with a
faint downward tilt, holding a GRID of small octagonal specimen-cards recessed into the metal,
organized in labeled rows (families). A discovery counter is debossed at the top. Vast quiet plate
margins. This is the catalogue of the whole autonomous entity.

SUBJECT — top to bottom:
1. HEADER, debossed: left "THE CODEX", right "DISCOVERED 14 / 47".
2. ROW A, small debossed row label "ORGANS": six octagonal cards, each a debossed glyph + name —
   "MOUTH" "GOVERNOR" "MEMORY" "LOOP" lit warm amber (discovered); "SENSES" "HANDS" dark fog cards
   (undiscovered, empty recessed slots).
3. ROW B, label "PARTS": eight octagonal cards — "THE DRAW" "THE FRAME" "THE METER" "THE LADDER"
   "THE MEASURE" "RECALL" lit amber; two trailing cards blank fog with only a debossed "?".
4. ROW C, label "DOCTRINES": four cards — "RESTORABLE COHERENCE" "BACKWARDS CHANNEL" lit amber;
   two fog cards with "?".
5. A faint debossed footnote, bottom edge: "EVERY ENTRY IS EARNED BY A REAL RUN".

KEY DETAILS
- Discovered cards: the octagonal chip face with a debossed glyph, the name, and a faint warm
  seat-glow from beneath — alive. Undiscovered cards: dark recessed empty slots, a single debossed
  "?", no glow — the fog state, applied to catalog entries.
- The GRID is precise: rows aligned, cards evenly spaced, an engineer's specimen wall — a periodic
  table of a mind, not a game inventory of loot.
- Amber appears ONLY as the seat-glow of discovered cards; all text debossed structure-grey.

CONSTRAINTS (hard)
1. Landscape 1536×1024, high quality (many small labels), face-on with a faint tilt.
2. Three labeled rows (ORGANS ×6, PARTS ×8, DOCTRINES ×4); the discovered/fog split exactly as
   listed; the counter reads "DISCOVERED 14 / 47".
3. Amber only on discovered cards' seat-glow; fog cards dark with a debossed "?"; all names in
   debossed structure-grey.
4. Every quoted name rendered once, verbatim, legible.
5. No people, no watermark, no gloss, no rounded UI, no new elements.

## Ready-made refinement lines

- "Change only the fog cards: darker and emptier, just a debossed '?'. Keep everything else the same."
- "Change only the grid: rows more precisely aligned, even spacing. Keep everything else the same."
- "Violation — the counter must read exactly 'DISCOVERED 14 / 47'. Correct that string only, change nothing else."
- "Count violation — ORGANS row exactly six cards, PARTS row exactly eight, DOCTRINES row exactly four. Regenerate with those counts, everything else the same."

## What this frame must make a stranger feel

*This game is DEEP — there is a whole taxonomy of a mind to discover, and I've barely begun.* If it
reads as a loot inventory, it fails; if it reads as a specimen wall you fill by earning, it passes.
