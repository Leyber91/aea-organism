# THE PROBE — THE MASTER MAP (the ordered strategy)

*The single source of truth for what we're building, in what order, held to which bar, under which lessons.
Everything discussed this arc is contemplated here or pointed to. When in doubt, this map decides the next
move. Updated 2026-07-24.*

## 0 · NORTH STAR

- **Thesis (locked):** wire living proofs of a mind, each more complete than the last, until you hold the
  whole one — and it keeps running after you close the tab.
- **The quality bar = the 12 locked references** (`design/refs/REFS.md`). **Build-to-match is the definition
  of done:** a visual slice ships only when the render matches its reference side-by-side. No less.
- **The spine = the honesty law + claim ceiling:** every value is live truth or a dash (never a guess/0);
  a proof is a receipt (it ran), never a claim; MOUTH not BRAIN; no cosmetic without a real event.

## 1 · WHERE WE ARE (verified state, 2026-07-24)

- **BUILT + VERIFIED:** THE MERGE works — fly → dock (F at the core) → seat → ignite → real receipt, as ONE
  program. **COMPOSED 0 → 1** (`r-01`, a real draw). **Both plants fire:** free hearth (`ollama/qwen2.5:7b`,
  `COST FREE`) and paid reach (`nvidia/mistral-small-119b`, `COST 1u`, PASS) — the economy is real. Bench
  refine **pass 1** done (opaque machined plate, octagonal chips, seat-glow — the world-wash killed).
- **REFERENCED (the bar):** REF-01…12 locked. The vision posters (`refs/vision/WHOLE_GAME.md`) authored.
- **DESIGNED (scope):** the full game — `GAME_INVENTORY.md` (the element BOM) + `THE_FIELD_GUIDE.md` (the
  scenario/book guide: the 3 apertures, the antagonist, the economy, the menus, the mind metaphor).
- **THE HONEST TRUTH:** COMPOSED just went 0→1. Everything past the guided minimal loop is design/fog until
  built. The discipline: **build the loop beautiful and honest, then earn each next layer.**

## 2 · THE ORDERED BUILD PATH (the strategy — guided-first, build-to-match)

### PHASE A — MAKE THE FIRST LOOP BEAUTIFUL (the guided minimal loop → the REF bar)
1. THE MERGE + first fire — **DONE.**
2. **Bench plate → REF-10/07** (refine passes, side-by-side): pass 1 done (machined plate). Next: the
   continuous **milled-rail groove**, bigger emphatically-octagonal chips, the **live amber packet** on RUN.
3. **The receipt/economy surfaces → REF-11/05/08:** the run-trace as a column not a table, the ladder with
   EST-before-fire, the fall-through as a milled groove stepping down a dead module (warm-amber `FELL
   THROUGH`, never red).
4. **Calm the dock composition** (the core no longer washes the plate — done at the material level; refine
   the dock-camera framing so the plate sits against near-black).
5. **The honesty fixes (correctness):** `tokens 0 → a dash` (`energy.py:152`), the `len>0` MEMORY over-claim
   (`schema.py`), the `alive` field folded into the claim-ceiling lint (scan JSON/code, not just copy).

### PHASE B — CLOSE THE GUIDED ARC (rung 2, the first earned name)
6. **RUNG 2 · RECALL forge** — a real Claude+Luis dev session; forge `RECALL` as a fireable part + **raise the
   MEMORY bar** (light on a recall that changed a draw, not `len>0`).
7. **The earned-title ceremony → REF-09** — signature + receipt, the one lawful amber text. First title fires
   ONLY on a real measured delta (grounded-vs-not once RECALL forges, or the real LAST/BEST delta today).
8. **The four aha moments** — first amber · first fall-through · first refusal-held (warm-amber) · first title.

### PHASE C — THE META MADE VISIBLE (R43's missing half)
9. **The journey/evolution map + the codex** — built FROM REF-01's locked law + the reveal ledger + the sacred
   save. **NOT new reference generations** (lesson: don't re-run the forge — that's avoidance).
10. **Visibly distinct stage silhouettes** (SPARK / METABOLISM / PERCEPTION — topology change, not more dots).
11. **The autonomy-battery character-sheet readout** — the #1 faithfulness hole; the `/autonomy` endpoint is
    already live (a read, not new machinery). Never shows Class 2 before a real endurance run.

### PHASE D — THE GUIDED FRAME + CLOSE
12. LEYBER voice (terminal-register, ceiling-disciplined) · save/resume to the exact beacon · reduced-motion
    (a hard law) · the one-line **aperture indicator** (`GUIDED · SANDBOX`) · a clean **graduation** beat that
    NAMES BUILDER/ARCHITECT/LIVE as cold-blue fog · lock the guided curriculum subset · cover/defer axis.A.

### DEFERRED — named as fog, built as NOTHING now (the sequencing gate)
BUILDER + ARCHITECT apparatus · the runaway/corruption/ruins antagonist ecology (needs a running construct
first) · the trace vault / replay ("catch creatures") · the depth-dial / mode-preset machinery · rungs 3–7 ·
the 12 non-firing parts · the being-catalog/registry/manifest · generating REF-13…17. **A threat with nothing
to threaten, or a catalog for 0 constructs, is corpus around the empty center — forbidden until earned.**

## 3 · THE FULL SCOPE, CONTEMPLATED (nothing lost)

| system | decision | when | lives in |
|---|---|---|---|
| **The 3 apertures** | one engine, three depths (FIRE→WIRE→PROGRAM), one save | GUIDED now; BUILDER/ARCHITECT = fog | field guide |
| **The mind metaphor** | concentric = TRUST (radius=zone strictness); the FOCUS RING (one object, no menus) | build target for the world | field guide |
| **The probe** | stays a 3%-speck forever; you level the WORLD not yourself; earns grey reach-notches, not amber | canon, never re-litigated | field guide |
| **The antagonist** | honest, no monsters/no red: starvation · cold-matter · the wall | THE WALL now; runaway/rot/corruption = fog (sequencing-gated) | field guide |
| **The economy** | CS-class: `COUNT×FREQ×COST ≤ capacity`; one currency (a call); free hearth vs paid reach | STAKE **verified**; full grid-sim = BUILDER+ | field guide, R44 |
| **The menus (8)** | bench · world · ladder · codex · being-sheet · stats · settings; reveal-not-restructure | bench now; map/codex = Phase C; rest = fog | inventory, field guide |
| **The progression** | 8-rung completeness ladder; each rung a different silhouette | guided closes on rung 2 | apologia, field guide |
| **The composition universe** | viability spectrum (most beings fail = the teaching); earned-not-authored | guided hides the 17-space | field guide, apologia |
| **The vocabulary** | 4 namespaces (rungs/organs/parts/titles); MOUTH/THE DRAW/etc. | locked | A17_VOCABULARY |

## 4 · THE LESSONS (standing rules — never re-learn)

1. **BUILD-TO-MATCH.** The 12 references are the bar; a slice is done only when it matches its ref
   side-by-side. "No less."
2. **COMPLETION OVER CORPUS.** Don't design around the empty center. The sequencing gate: no construct-threat,
   no catalog, before a real construct exists. Build the loop, then decorate. (The repo's recorded failure is
   110:1 words-to-code; two panels this arc both ended "stop writing, ship the merge.")
3. **THE IMAGE-GEN PROTOCOL** (`refs/bundle/00_HOW_TO_USE.md`, 9 field lessons): anchor image + canon block ·
   spec MATTER not information · counts stated twice · preempt model tics in the base prompt · fresh-gen beats
   edit-chains · reference-reuse (no re-attach within a conversation) · adopt-back what the model does better.
4. **HONESTY IS GRAMMAR.** Every value real or a dash; MOUTH not BRAIN; no cosmetic without a real event; the
   claim ceiling is a voice, not a label. The three live breaches (tokens=0, len>0 MEMORY, `alive`) are FIXES,
   not carries.
5. **VERIFY THE REAL CODE.** THE MERGE was already built this session — the panels read stale docs. Read the
   actual files; test the actual loop; "done" means it ran (a screenshot / a receipt), never "it compiles in
   my head."
6. **BOTH PLANTS FIRE.** Free hearth + paid nvidia reach both make real draws — the economy and the STAKE are
   real, not simulated.

## 5 · DOC INDEX (where everything lives)

`design/ROADMAP.md` (this map — the ordered strategy) · `PRODUCTION_PLAN.md` (HOW it's built: the data-driven pipeline + the systems) · `GAME_INVENTORY.md` (the element BOM + guided
checklist + critic verdict) · `THE_FIELD_GUIDE.md` (the scenario / book guide) · `A17_VOCABULARY.md` (the
names) · `AEA_APOLOGIA.html` (the AEA theory) · `refs/REFS.md` (the 12 locked references = the bar) ·
`refs/vision/WHOLE_GAME.md` (the whole-game vision posters) · `refs/bundle/` (per-image specs + the image
protocol) · `diary/SESSION_LOG.md` (running state) · `diary/REFLECTIONS.md` (R1–R44, Luis's raw sparks) ·
`graph.json` (the knowledge graph — refresh with `python -m aea.tooling.build_graph`).

**The next move is always the lowest unchecked item in §2.** Right now: **Phase A, step 2 — the bench plate
to REF-10 (milled rail + bigger octagonal chips + the live RUN packet).**
