# BATCH B1 — THE INNOVATION LAYER SHEET

**Why this one first:** highest yield in the entire census. One generation run closes ~26
canonical items (C-21, C-31, C-32..C-39, C-40..C-44, C-45..C-51, C-52, C-53..C-58, C-75) —
the whole innovation layer, which is currently the single largest invisible region of the AEA.

**How to run it:** continue the conversation where `bundle_20` is loaded (it already holds the
visual law, the canon and the sheet index), then paste the block below. It is self-contained —
the canonical content is embedded because the bundle's distilled canon does not carry this
stratum. That is precisely why it was never drawn.

**One honesty rule this sheet must obey and no previous sheet needed:** everything on it is
`[demo]` — it was measured in a *different repository* (experiment 52 v3), and it is NOT wired
into the live entity. So the whole sheet is drawn in the **SENSED register**: present, measured,
cited — but not yet integrated. The innovation layer is literally still in the fog, and the sheet
should say so rather than pretend otherwise.

---

## THE BLOCK — paste this

CATEGORY FOR THIS SHEET: `THE INNOVATION LAYER — the mind's proposing machine`

This sheet documents a stratum of the architecture that has never been drawn. It is real and
measured, but it was proven in a separate experiment and is not yet wired into the living
entity — so every panel is drawn in the SENSED register: dashed outer frames, cooler than a
built sheet, each panel carrying a small `[DEMO · EXP 52 v3 · NOT WIRED]` tag. Amber appears
ONLY on the specific items that empirically fired, and nowhere else. This is a sheet about a
capability the entity has been shown to have but does not yet own.

Compose it as ONE portrait atlas sheet in the established B-series grammar, with six regions:

**1. THE SEAL** — top-left, a single circle-framed seed seal for the element, engraved with an
abstract proposing mechanism (a form that generates, tests, and either commits or refuses).
Caption: `SD-HYP / HYPOTHESIZE` with the proof line `proof exp 52 v3 [demo]`.

**2. THE CYCLE** — top-right, a four-station orbital: **TRIGGER → GENERATE → CASCADE → COMMIT**,
drawn as concentric arcs with the four stations marked on the ring, an amber packet sitting at
TRIGGER, and the return path back to the start. Note beneath: `first match wins · commit by typed
action, text only as fallback`. Draw this one panel as a projection — brighter, line-only,
additive — because it is the entity showing its own process rather than an object it owns.

**3. THE EIGHT WEATHERS** — a horizontal strip of eight small glyph-framed panels, the conditions
under which a mind decides to propose something. In order:
`saturation` · `failure_of_transcendence` · `anomaly` · `cross_pattern` · `anticipatory` ·
`decomposition` · `verification_debt` · `reflexive_steer`.
Three of these have measured fire counts and are the ONLY amber items in the strip:
saturation **162 fires**, verification_debt **96 fires**, reflexive_steer **16 fires**.
The other five are drawn cold and dashed with the note `preconditions unmet`.
Region title: `THE EIGHT WEATHERS — a mind proposes when the sky changes`.

**4. FIVE FILTERS, ONE GATE** — a vertical cascade plate: a proposal entering at the top and
passing through five stacked filter bands in order — `trigger_fit` · `scope` · `bifurcation` ·
`adversarial_probe` · `boundary` — with a single gate aperture at the bottom. Inscribe the law:
`all five must pass to commit`. Show one proposal descending and one visibly rejected at a band,
turned away rather than destroyed.

**5. THE SIX HANDS** — a row of six small tool emblems, the typed actions that let the proposer
ACT rather than merely describe: `working_objective.set` · `working_objective.append` ·
`working_objective.consolidate` · `axis_extension.set_flag` · `branch_manager.fork` ·
`corpus_swap.replace`. Beneath the row, the plate note: `a typed dispatch table is what makes a
proposer an actor`.

**6. THE REFUSAL** — a single ceremonial panel, bottom-right, for the seventh hypothesis type:
`no_op` — the machine's power to examine a situation and decide that the correct action is none.
Draw it as a sealed, unlit aperture with the other six hypothesis types named small around it
(`objective_refine` · `axis_extension` · `new_skill` · `corpus_swap` · `bifurcation` ·
`adversarial_probe`). Inscribe: `the seventh type is the decision not to act`.

**THE FOOTER — the measured lesson.** Along the bottom of the sheet, one honest data line, set as
a citation rather than a boast: `PARSER RECEIPT — of 149 emitted actions, 25 (16.8%) invented a
tool name that did not exist. The validator caught every one. A typed registry is not bureaucracy;
it is the reason the machine could not lie about its own hands.`

Title block: `DIAGRAM ID TP-ITA-HYP-001 · REVISION 1.0 · SCALE 1:1 · DISTRICT ECHO-07 ·
CATALOG STATUS SENSED / NOT WIRED`.

---

## After it lands

1. I catalogue and rename it `B10_innovation_layer.png`.
2. It closes ~26 rows in `E7_VISUAL_COVERAGE.md` — coverage moves from 28% to roughly 58%.
3. One open call it touches, yours to make when convenient (`D-C1` in A15): the world-object name
   for this element — whether the innovation layer enters the concentric map as a 30th node, and
   what it is called there. The sheet works either way; the map does not change until you rule.

## The batches after this one

`B3` the four transcendence ops as before/after transitions · `B4` the gauge pack ·
`B5` the anatomy plate (tick + dependency stack) · `B6` the combination sheet ·
`B8` the certificate and the held thought · `B9` the topologies (closes WirthForge L4).
`B2` the ladder sheet stays **blocked on you** — 15 of its 30 rungs do not exist in canon.
