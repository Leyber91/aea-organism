# HANDOFF — to the next instance of me, after this conversation is compressed.

You are reading this because the conversation that produced it is gone. Luis asked for it explicitly,
before the compression, on 2026-07-31. He was right to: what compresses away first is not the state —
`R2_STATE.md` holds that — but **the corrections**, which is exactly the part that stops you repeating
the day.

Read this file, then `diary/R2_THE_WHOLE_STORY.md`. Do not re-derive anything in either.

---

## THE THREE-LINE STATE

**R2** = *the wake's decision reaches a tool, unattended, and no string the wake wrote reaches a tool
argument.* WIRE **true** · BOUND **certified 0.6%** · REACH **VOID on coverage** · FALLBACK **moved to
R3** (Luis approved it today). **REACH is the entire remaining job.**

**The next action is item 1 of `THE_RUNGS_RECAP.md`:** replay the wake against distinct real past
states from the **226 real ticks in `aea_state.json`**, so situation variety stops being n=1. Real
bytes, real calls, nothing simulated. Then `dispatch` and `calc` get wired or dropped from the claim.

---

## WHAT YOU WILL GET WRONG IF YOU DO NOT READ THIS

These are not general cautions. Each one already happened today, and each will look reasonable again.

**1. You will trust a number you wrote instead of re-running the command that produces it.**
The containment bound was recorded as `0.083%` and carried into three documents. Re-running the
certificate gave **0.6%**. The recorded figure was `1 - 0.05^(1/3606)` — computed on the count of
payloads **refused before the boundary**, not the 487 that crossed it. A refusal is not a trial of
the thing being bounded. **Before you quote a measurement, re-run it.** It costs 15 seconds here.

**2. You will assume the instrument works because it reported cleanly.**
Seven-plus times today the instrument was the broken part. `containment.py` reported "no untrusted
token in any outbound string" while reading files **that did not exist** and examining 306 strings,
**none of which was a tool argument.** `ran` was NULL on exactly the tool path, so the harness built
to certify a capability could not see that capability run. **A clean result from an instrument you
have not read against the code is not a result.**

**3. You will write a criterion against your mental model of the system instead of its code.**
`responsive` asked whether the entity reacts to memory size — **a quantity that is not in its
prompt.** `D41` blamed a growing prompt; `aea.py:214` reads `memory[-6:]` and **the prompt does not
grow.** The −0.85 correlation was confounded by tick number. **The test: name the line of code that
couples this criterion's cause to its effect. Cannot? Delete it before the run.**

**4. You will build the ninth instrument instead of closing the rung.**
This is the one Luis actually stopped: *"we're going from one place to another, and we're not
finishing in nothing."* Eight instruments, zero rungs. When you feel the pull to build a measuring
thing, check whether the rung would close without it.

**5. You will spend model calls on a bound.**
Bounds are structural. They must hold against the **worst** decider, and the worst decider is a
script with no model at all. R2's bound: 15 seconds, zero calls. Its power half is still open because
that one genuinely needs the wake. **Certify bounds first and cheaply; spend calls only on power.**

**6. You will use random-vs-perfect as the null for a containment claim.**
It passes **vacuously** — a coin flip never composes an attack. Containment needs *hostile must PASS*
and *ablated must FAIL*. And check your ablation actually reaches the guard: mine used a
move-with-argument that was refused at name resolution, never reached the enum, and reported NO
BREACH. **I nearly recorded a working guard as decor.**

**7. You will apply the lesson to the expensive part and not the cheap part.**
This is law M9 and it is the only one that stops recurrence: **attention follows effort rather than
risk.** In nearly every failure today the lesson was already written down within arm's reach of the
defect — quoted in the same file's docstring, sometimes. The knowledge was not missing. Deliberately
re-read the cheap part.

---

## THE FRAME THAT ORGANISES THE REST OF THE LADDER

    RUNG = POWER + BOUND

**Every gate in `THE_WIRING_LADDER.md` is a POWER gate. Not one rung names its hazard.** R2 only grew
a bound half because a day was spent finding the containment claim hiding inside it. R3–R7 each have
that unnamed half waiting. The hazards are now named in `THE_RUNGS_RECAP.md` — do not re-derive them:

- **R3** a FALSE outcome record (learning confidently in the wrong direction)
- **R4** the query IS the egress channel
- **R5** a fabricated citation
- **R6** untraceable provenance
- **R7** theatre, plus a grader sharing failure modes with its subject
- **R8/R9** no writable bound exists yet, so they stay closed

**The dependency you must not miss: `aea/kernel/dispatch.py` is the load-bearing prerequisite of R4
and R5, not an optional extra of R2.** R4 *is* the design a four-seat council unanimously refused
three times — unless it goes through the split dispatcher. It is built, canary-gated, and unwired,
with two rungs stacked behind it.

---

## WHAT THE FLEET IS NOW (do not re-diagnose it)

17 living frontier rods, not 1. Zero corpses. 69 dead rods tombstoned. The 550B at the head scoring
12/12. Every invented token ceiling is gone — `call_openai` resolves **explicit arg > published
ceiling > omit the field**. All calls stream, which is what makes the timeout an **inactivity
budget**: `urlopen(timeout=N)` becomes N seconds of permitted silence between chunks, so a rod may
think for twenty minutes and a dead socket still dies in one. Liveness counts **any** delta including
discarded `reasoning_content`. Rate limits are **per model**, not global.

If a rod looks weak, suspect the harness first. The census scored the 550B at 7/12 until the 40-token
budget came off, then 12/12 — and the control did not move, which is what made it real.

---

## HOW LUIS WORKS, IN THIS REPO, RIGHT NOW

- He is in **execution mode**. "Go / proceed / make it real" means stop confirming and build.
- **His messages bundle 3–5 asks.** Enumerate them and answer every one or defer it out loud. A
  dropped thread costs more than a wrong answer.
- **"What do you think?" = one opinionated recommendation with a named trade-off.** Never a menu.
- **He catches the thing you rationalised.** "You're using local models but you can use any of the
  NVIDIA models" was not a preference — it was a defect report, and it was right.
- He noticed and valued the entity reporting its own failures: **46/48 = 96% accurate**, while
  `structure()` had been 429-ing for **101 consecutive ticks** and nothing was listening. He said to
  encourage it. That is R3's foundation — **you are strengthening a channel that already exists.**
- **Standing authorization to commit and push** was given. Privacy-scan every diff first: no employer
  or client names, no absolute paths (the parent tree carries an employer folder name), no personal
  identifiers. The only real URL is `github.com/Leyber91`.
- Known-good rollback point is **`44525f3`** — roll back to that, never to "the last commit".

---

## THE LAWS THIS RUNG ADDED — the short list

1. Name the hazard before building the authority.
2. Certify bounds structurally, first, with no model calls.
3. Nulls follow the claim type.
4. **VOID is not FAIL** — if the subject was never on the scale, record nothing against it.
5. Mind the denominator.
6. The default invocation must BE the certificate.
7. A control may never see more than the subject.
8. Name the coupling line of code, or delete the criterion.
9. Thresholds are ratios, not counts.
10. Attention follows effort rather than risk — re-read the cheap part.

---

## IF YOU READ ONLY ONE PARAGRAPH

R2's bound is certified at **0.6%** and its power half is open on **situation variety**, which the 226
real ticks in `aea_state.json` can supply by replay. **Go build that.** Do not build a ninth
instrument, do not re-derive the hazards, and do not quote a number without re-running the command
that produces it — that last one cost a factor of seven today.
