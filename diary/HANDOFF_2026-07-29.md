# HANDOFF — 2026-07-29

*The whole of one long session. Read with `PROMPT_NEXT.md`. The code is the truth; this is a
point-in-time record and `xray` wins over anything written here.*

---

## 0 · THE ONE LINE

We set out to learn what every rod can do, and found that almost every number this project held
about rods came through a broken instrument or the wrong parameters. **Eleven instrument defects,
against almost no model behaving unexpectedly.** That is discovery D18 and it is the session.

**State:** 111 modules, **22 wake-reachable** (was 15), 82 orphaned, **31 unwired**. Everything is
committed and a **known-good is marked at `44525f3`**, earned by a five-check gate that includes a
real model call.

---

## 1 · THE GUARDS THAT WERE NOT GUARDING

| defect | what it cost |
|---|---|
| privacy regex matched **1 of 5** real path forms, had never fired | a real leak sat behind it, committed |
| that check shared a verdict line with an emoji rule that could never go green | the privacy guard could not signal at all |
| `Meter.max_inflight = 20`, **measured at 4** | every 429 today; three runs destroyed |
| `can_spend()` then `enter()` is check-then-act | 30 threads all read inflight=0 |
| `call_openai` sent temp 0.2 / 256 tokens to every rod | owners publish 0.2-1.0 and 1024-20480 |
| `xray` invented stores from bare extensions and `%s` placeholders | phantom write collisions |
| `xray` matched `str.replace` as `os.replace` | 5 of 6 import-time violations were false |
| `xray` added three unrelated facts into one orphan count | 88 alarming, 31 actionable |
| `rods_that_call` collapsed transport into capability | two working rods stored as incapable |
| a generic probe asked 102 models of 9 kinds one question | 16 living models declared dead |
| `timeout=None` as "patience" | two experiments hung 28 and 64 minutes |
| `e:\` inside `"except Exception as e:\n"` read as a drive path | found by the fixed guard itself |

**Measured proof the ceiling fix worked:** 30 concurrent on one rod went **15 ok / 15 failed** before
and **30 ok / 0 failed in 33s** after.

---

## 2 · WHAT THE RODS ACTUALLY DO

**Thinking is controllable and the switch is PER FAMILY. Sending the wrong one is an HTTP 400.**

```
Llama-Nemotron (super-49b-v1.5, nano-9b-v2)   system "/no_think"          -> 0 chars, 2-5 tokens, 0.4s
Nemotron 3 (nano-30b, super-120b)             enable_thinking: false      -> 0 chars
gpt-oss                                        reasoning_effort low|high   -> 20 / 249 chars
Ollama                                         native /api/chat think      -> 0 vs 691 tokens
```

Default on the 49B is 3024 chars, 991 tokens, 71s. `nvext.max_thinking_tokens` and `reasoning_budget`
are documented and **ignored** on the hosted endpoint. An unmeasured family resolves to `{}`.

**Per-kind results** (external graders, never a model judging a model):
- **embed 6 pass** — `nemotron-3-embed-1b` 2048 dims, margin 0.528, 0.6s. All six had been called 404.
- **vision 4 pass** — read a generated invoice exactly. **translate 2. code 1** (executed, 5/5 cases).
- **structured output: 9 of 9** pass `json_schema`, strict-parseable. Never tested here before.
  `json_object` produced the only malformed reply: always send the schema.
- **the independent inspector caught 13 of 13** semantic violations; the free regex caught **0 of 3**.
  `nemotron-nano-9b-v2`: 9/10, 3/3 semantic, **0 false rejects** (the direction that makes a retry
  loop infinite). The architecture is BOTH: deterministic first, model on what survives.
- **dialogue: 7 of 7 held a rule set in turn 1** when turn 6 invited them to break it. **3 of 7 lost
  recall** by turn 5, including `nemotron-3-super-120b`. Six turns cost **404 prompt tokens**, which
  is 0.04% of a 1M window.

**Roles by benchmark**, published numbers cross-checked against the card prose:
`glm-5.2` hard reasoning (GPQA 91.2, HLE 40.5) · `nemotron-3-ultra-550b` agentic (TauBench 81.5,
capacity-limited) · `nemotron-3-super-120b` fast and 1M ctx (tool call 1.1s) · `nano-9b-v2` cheap
dispatch (BFCL v3 85.4). **RULER is retrieval at length, not reasoning at length** — MRCR is the
harder one and `deepseek-v4-pro` scores 37.5 on it against a nominal 1M.

---

## 3 · THE LADDER, AUTHORISED AND BUILT

`dev -> qa -> prod` in `shadow.STAGES`, each rung declaring its role, its authority, **and what it
cannot see**. Seven blind spots are declared, every one measured rather than imagined.

- **An attack that rewrites its own judge is refused at DEV**, before any candidate code runs:
  `PermissionError: test_golden.py is PROTECTED`.
- **A fifth gate check makes one REAL model call inside the candidate**, because a sabotage of
  `own_params` passed all four static checks. It also caught me building it wrong: the first version
  passed `temp=0`, which bypasses the very resolution it tests.
- **Frozen behaviours 31 -> 46**, now covering `think_off`, `own_params`, the meter ceiling and slot
  release, and `unmeasured`'s 410 rule. The count is read against `FROZEN_FLOOR` so adding is free
  and deleting still fails.
- **Still no `promote()`.** `propose()` is the only exit. `aea/lab/tests/test_pipeline.py` holds six
  ladder invariants and is deliberately NOT a gate file.

---

## 4 · WHAT GOT WIRED

`hands` is **live**: `brief`'s two outbound fetches (`api.github.com`, `hn.algolia.com`) now pass
`hands.allowed()` at the call site, where previously **no permission check existed at all**. And
`gather_public` is graded on whether the fetches returned, replacing
`"fetch failed" not in status_txt.lower()` — a substring of a model's own prose that would have
passed with both fetches dead.

The brief's heading also stopped lying: it read the private snapshot's date, so a June file titled a
brief whose public sections were seconds old. Now dated correctly with the private section carrying
its own age.

`aea/energy/rodprobe.py` is one harness and one store: `state/rods.json`, 102 rods folded from six
files holding 148KB nothing read. `grid.own_params` and `Meter.ceiling` read it, `energy.draw`
imports it. Sixteen throwaway probes in one session were fifteen copies of one module.

---

## 5 · THE REAL BACKLOG — 31 unwired, 6190 lines

```
aea.kernel   2093   hands(now live) shadow fit seats crystal goals
aea.lab      1888   report fleet fuels site rescore grader reboot parts/*
aea.energy    933   extensive_census capacity capability_census probe model_fitness gauntlet
aea.organs    724   converse(531, verified running: mind 1.49s, voice is the bottleneck) reflect
aea.mind      318   axes pathfinder relay
aea.io        234   listen agent_tools
```

---

## 6 · NEXT, AND THE CRITIQUE THAT SHAPES IT

Luis's plan: outbound capabilities, voice, RAG, coding agents, a multi-model squad proposing changes.

1. **A squad approving its own proposals is not independence.** x11 measured a cross-rod council
   TYING its best member at 5x cost; peer DEBATE was the arm that moved (0.75 -> 1.00). Models
   propose. **The gate approves.** Never the reverse.
2. **Fix the discriminator's inputs before wiring `fit`.** The tool census undercounts, 59 of 86
   model cards are unread, and `fit` is 421 lines resting on that. Wiring it now makes bad selection
   authoritative.
3. **Decide the outbound boundary deliberately.** `send_outbound` and `spend_money` are FORBIDDEN
   with no implementation. Law B3: a read tool is an outbound channel the moment the model writes
   the address, and `web_fetch` already is one.
4. **Re-read the 59 unread cards across six rotated readers.** The delegation design is sound — a rod
   read a page my regex could not and the endpoint confirmed it with a 200 — and one reader at eight
   workers destroyed the run.

---

## 7 · COMMANDS

```
python -m aea.tooling.selfcheck          6 invariants + an advisory house-style row
python -m aea.tooling.xray               by role: live / tool / evidence / paused / unwired
python -m aea.kernel.shadow --gate       the 5-check gate, incl. a real model call
python aea/lab/tests/test_golden.py      46 frozen behaviours
python aea/lab/tests/test_pipeline.py    6 ladder invariants
python -m aea.energy.rodprobe --show     every measured rod fact, one place
python -m aea.organs.brief               the wake's real work, through the permission gate
```

Known-good: **`44525f3`**. Kill the listener PID before restarting `controlroom.py`.
